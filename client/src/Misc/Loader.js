import React from "react";

const Loader = () => {
    return (
        <div className="loading-spinner d-flex flex-row justify-content-center">
            <i className="fas fa-spinner fa-spin"/>
        </div>
    );
};

export default Loader;
